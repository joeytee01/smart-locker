<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmartLockerSolution" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Disable default button background tint -->
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
    </style>

    <style name="Theme.SmartLockerSolution" parent="Base.Theme.SmartLockerSolution" />

    <!-- Custom button style -->
    <style name="Widget.App.Button" parent="Widget.Material3.Button">
        <item name="backgroundTint">@null</item> <!-- Disable background tint -->
        <item name="android:background">@drawable/locker_button_background</item> <!-- Use custom drawable -->
        <item name="android:textColor">@android:color/black</item> <!-- Set text color -->
        <item name="android:padding">8dp</item> <!-- Add padding -->
    </style>

    <style name="CustomDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowCloseOnTouchOutside">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowMinWidthMajor">800dp</item>
        <item name="android:windowMinWidthMinor">800dp</item>

    </style>

    <style name="ErrorToastAnimation">
        <item name="android:windowEnterAnimation">@anim/error_toast_slide_in</item>
        <item name="android:windowExitAnimation">@anim/error_toast_slide_out</item>
    </style>
</resources>